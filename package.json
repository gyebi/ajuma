{
  "name": "ajuma-ai",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace frontend\" \"npm run dev --workspace backend\"",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "tunnel:frontend": "ngrok start frontend",
    "tunnel:backend": "ngrok start backend",
    "tunnel": "ngrok start --all",
    "dev:tunnel": "concurrently \"npm run dev\" \"npm run tunnel\"",
    "build": "npm run build --workspace frontend && npm run build --workspace backend && npm run build --prefix functions",
    "start": "npm run start --workspace backend",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace backend && npm run lint --prefix functions"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "axios": "^1.15.2",
    "dotenv": "^17.4.2",
    "firebase-admin": "^13.7.0"
  }
}
